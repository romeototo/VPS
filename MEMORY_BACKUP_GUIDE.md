# 🧠 Memory Backup System

ระบบ backup ความจำขึ้น GitHub ป้องกันการสูญหายเมื่อ session หมดอายุ

---

## 📁 โครงสร้างไฟล์ที่ Backup

```
memory/
├── YYYY-MM-DD.md     # Daily logs (auto-generated)
└── heartbeat-state.json  # สถานะล่าสุด

MEMORY.md             # ความทรงจำระยะยาว (curated)
AGENTS.md             # กฎการทำงาน
SOUL.md               # บุคลิก/ตัวตน
USER.md               # ข้อมูลผู้ใช้
IDENTITY.md           # ข้อมูลตัวเอง
TOOLS.md              # เครื่องมือและการตั้งค่า
```

---

## 🔄 วิธี Backup (มี 2 ระบบทำงานพร้อมกัน)

### ระบบที่ 1: Cron Job (ทุก 30 นาที)
- รันอัตโนมัติโดยไม่ต้องทำอะไร
- แจ้งผลใน chat เมื่อ backup สำเร็จ/ล้มเหลว

### ระบบที่ 2: Heartbeat (ตอนได้รับ heartbeat)
- Check ว่าครั้งล่าสุด backup นานแค่ไหนแล้ว
- ถ้าเกิน 1 ชั่วโมง → backup ทันที

---

## 🚀 การตั้งค่าครั้งแรก

### 1. สร้าง GitHub Repository
```bash
# บนเว็บ GitHub → New Repository
# ตั้งชื่อ: openclaw-memory หรือชื่อที่ต้องการ
# อย่าติ๊ก "Add README" (เราจะ push เอง)
```

### 2. เชื่อม Remote
```bash
cd /home/z/.openclaw/workspace
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### 3. ใส่ Git Credentials (ถ้าถาม password)
- ใช้ **GitHub Personal Access Token** แทน password
- สร้างที่: GitHub → Settings → Developer settings → Personal access tokens

### 4. Push ครั้งแรก
```bash
git add .
git commit -m "Initial: OpenClaw memory system"
git push -u origin master
```

---

## 📋 คำสั่งที่ใช้บ่อย

| คำสั่ง | ใช้ทำอะไร |
|--------|-----------|
| `git status` | เช็คสถานะ |
| `git add memory/ MEMORY.md` | เตรียมไฟล์ |
| `git commit -m "message"` | บันทึก |
| `git push` | ส่งขึ้น GitHub |
| `git pull` | ดึงจาก GitHub (session ใหม่) |

---

## ⚡ Manual Backup (บังคับ backup ทันที)

ถ้าอยาก backup ตอนนี้เลย:

```bash
cd /home/z/.openclaw/workspace
git add memory/ MEMORY.md AGENTS.md SOUL.md USER.md IDENTITY.md TOOLS.md
git commit -m "manual: memory backup $(date '+%Y-%m-%d %H:%M')" --allow-empty
git push origin master
```

---

## 🆘 กู้คืนความจำ (หลัง session ใหม่เริ่ม)

```bash
# 1. Clone repo ใหม่ (ถ้าเครื่องใหม่)
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 2. หรือ pull ถ้ามีอยู่แล้ว
cd /home/z/.openclaw/workspace
git pull origin master
```

---

## 📝 หมายเหตุ

- **อย่า backup ไฟล์ลับ** (รหัสผ่าน, API keys) → เก็บใน `.env` หรือ `.gitignore`
- **Commit message** จะมี timestamp อัตโนมัติ
- **ถ้า push ไม่ได้** → เช็คว่า remote ถูกต้องไหม
- **ถ้าถาม password** → ใช้ GitHub Personal Access Token

---

*ตั้งค่าครั้งเดียว ใช้ได้ตลอดไป* 🎉
